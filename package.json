{
  "name": "clash_cleaner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/content.ts src/page_script.ts src/background.ts --bundle --outdir=dist --target=chrome100",
    "watch": "esbuild src/content.ts src/page_script.ts src/background.ts --bundle --outdir=dist --target=chrome100 --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
